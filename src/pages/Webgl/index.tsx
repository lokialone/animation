import React, {useRef} from 'react';

const Home = (props: {path: string}) => {
    // useEffect(() => {
    //     effect;
    //     return () => {
    //         cleanup;
    //     };
    // }, [input]);
    return <canvas id="gl"></canvas>;
};

export default Home;
